# Часть 1 — промежуточная работа, не тестовый релиз

Продолжение от `6c39dc383e62a284be58f2eb50a6bb85b24f0c64`, только `work/v06-commercial-rebuild`.
Текущая контрольная точка исправлений окружения: `305edd4b3eb02252a77322dfec776bc67b104ad4`.
Предыдущая проверенная точка подключения клиента и Windows: `9b564e96179c6222ace064a9f44a9d5d376b3fa3`.
Статус: `in_progress`. Части 2 и 3 не начаты.

## Продолжение в новом чате — визуальная проверка природы

Удалось получить и вручную просмотреть прежние runtime PNG из CI природы, замка и подключённого клиента. Это сняло прежнее ограничение чтения изображений. На кадре леса обнаружены почти исчезнувшие кроны сосен и чрезмерно длинные поваленные стволы; кадр скал показывал только землю. Поэтому прежний технический PASS загрузки не считается подтверждением качества окружения.

- Снова получен именно исходник Poly Haven Pine Tree 01 из согласованного реестра; размер и SHA-256 подтверждены. Глобальное упрощение разрозненных иголок уменьшало площадь хвои первого варианта с примерно 248 до 5 м² в координатах исходника. Вместо него `scripts/pine-foliage.py` использует исходную текстуру веточки и положения 1281, 775 и 1110 исходных веточек трёх деревьев. Стволы и древесные ветви сохраняются. Подготовлены по два уровня детализации, исправлен MIME для PNG с прозрачностью. Основной скрипт подготовки вызывает этот шаг автоматически.
- Поваленные стволы теперь ограничены по длине (3–7,6 м при существующем диапазоне размещения), их препятствие соответствует вытянутой форме. Четыре длинных скальных выхода используют повёрнутые прямоугольные границы вместо огромных круглых зон на свободной земле.
- Исправлен проверочный вид скал. Проверка снимков теперь обнаруживает старт внутри препятствия, сжатие камеры, потерю опоры и отсутствие нужного природного объекта в поле зрения. Кадр с одной землёй больше не считается достаточным свидетельством скал.
- Локально прошли production build, проверка ресурсов и 12 целевых тестов поверхности, столкновений, прохода замка и передачи карты. Серверный бой, прежняя заточка и Windows-упаковка этим изменением не затронуты и повторно не проверялись.
- [CI 34061047969](https://github.com/Pakka19941311/Pakka/actions/runs/34061047969) завершилась `success` на точном коде `305edd4b3eb02252a77322dfec776bc67b104ad4`: сборка, 12 целевых тестов, пять реальных видов окружения в Chromium software WebGL и два независимых построения карты. В пяти видах старт свободен, камера не сжата, персонаж имеет опору; нужные модели действительно находятся в поле зрения. Отчёты сохранены в `docs/qa/part1-environment-305edd4/`.
- Артефакт `part1-environment-review` (9997544802): 6 052 500 байт; SHA-256 `a322af13e836d4750a92799b5b036cad744140f0cf2c78c92d83fa8319e743bc`. Все пять PNG получены и вручную просмотрены. Кроны восстановлены, масштаб поваленных стволов исправлен. Вид скал теперь выявляет оставшийся дефект: полосы от растянутых текстурных координат упрощённого `coastal_cliff_04`. Технический PASS не означает исправление этого визуального дефекта или пользовательскую приёмку.
- Обновлённый `public/assets/world/world-topology.json` сохранён прямо из этого проверенного артефакта. SHA-256 `5c94270c590ae38e5be3a388a6dc404517dc4557e5eb51c45fc7c251ccb3b894`; 471 препятствие и 2 площадки, результаты двух независимых клиентов совпадают. Изменены 22 препятствия поваленных стволов и скальных выходов. Сохранение карты и свидетельств не меняет проверенный игровой код.
- Для исправления UV скалы подготовлен **непроверенный черновик** `docs/drafts/prepare-cliff-assets.mjs`, использующий `meshoptimizer@0.25.0` и исходные UV-атрибуты. Он не включён в сборку или автоматическую подготовку ресурсов; новые GLB скалы не созданы. Прямое получение согласованного исходного `coastal_cliff_04.bin` дважды отменено системой подтверждения сетевого доступа до принятия решения (`network approval was cancelled before a decision was returned`). Другие пути обхода этого ограничения не использовались. Поиск сохранённого исходника результата не дал.

Управление Cloud Browser в новом чате также возвращает `CDP operation refresh tabs timed out after 20000ms`; создание новой вкладки не восстановило связь. Это не повторный отказ входа. Четыре исходника House 001/002, Tavern 004 и Blacksmith 005 по-прежнему нужны для завершения части 1. Их можно передать файлами `.blend`/ZIP в чат, сохранив именно согласованные модели. Ни новый вариант художественного оформления, ни повторное согласование ТЗ из этого не требуются.

Следующая рабочая точка: получить четыре согласованных здания и исходный бинарный файл скалы, исправить UV с проверкой реального изображения, затем закончить архитектуру и обновить карту. Часть 1 не закончена; финальный Windows Release не выпущен. Части 2 и 3 не начаты.

## Продолжение после случайной остановки

- В производственном клиенте подключён `RemoteWorldGateway`: персонаж, противники, бой, добыча и предметные операции получают состояние от отдельного сервера. Локальная симуляция оставлена только в специальной сборке для проверки геометрии окружения; при потере сети производственная игра не переключается на неё.
- Серверная логика приведена к фактически принятым правилам выпуска атак, расхода MP, перезарядки, буфера навыка 350 мс, полёта снаряда 280 мс, точности, статусов и прыжка. Существующие шансы заточки и дропа не изменены.
- Подключены продолжение серверного героя, однократный перенос старого сохранения и восстановление ожидающей предметной операции с тем же идентификатором. Подтверждённая заточка не повторяется после потери ответа.
- Исправлена обработка потока состояния при медленном рендере: ограничение 2 МБ применяется к отдельному SSE-пакету, а не к сумме корректных пакетов в одном чтении; сервер учитывает остановку записи при заполнении сетевого буфера. Выбранный свиток сохраняется, если команда не отправлена из-за отсутствия связи. Подписи сущностей обновляются при изменении содержимого, без лишней перерисовки неизменившихся данных.
- Подготовлен Windows-пакет с собственным Node, запуском сервера и отдельной папкой сохранений. Прежний адрес `localhost:4173` сохраняется для доступа к старому браузерному сохранению. Подробности упаковки: `docs/PART1_WINDOWS_PACKAGE.md`.
- [CI 34056845965](https://github.com/Pakka19941311/Pakka/actions/runs/34056845965), код `9b564e96179c6222ace064a9f44a9d5d376b3fa3`: весь запуск — **SUCCESS**. Linux job `101550191415` подтвердил 28 целевых проверок, production build и шесть проверок реальных браузерных клиентов; Windows job `101550784066` подтвердил упаковку и нативный запуск распакованного комплекта.
- Повторная попытка связи с рабочим браузером завершилась тайм-аутами списка вкладок и перехода на страницу загрузки. Четыре согласованных файла BlendSwap по-прежнему отсутствуют. Вход в аккаунты повторять не требуется; получение зданий остаётся технически заблокированным.

Подтверждённые сценарии браузерной проверки текущего кода:

| Сценарий | Результат |
| --- | --- |
| Два игровых клиента подключены к общему серверу; первая страница заморожена | Серверный мир продолжает работу, второй клиент подключается |
| Возврат первой страницы | Тот же герой получает текущее состояние и видит второго героя |
| Удержание W и открытие Tab | Перемещение продолжается; проверена позиция на сервере |
| Двойной клик по обычному свитку оружия, затем клик по оружию | Одна безопасная попытка: оружие +1, свитков 99; повторного расхода нет |
| Перезагрузка и «Продолжить» | Сохраняются тот же герой, оружие +1 и 99 свитков |
| Закрытие обоих клиентов | Серверные часы продолжают идти |

[Артефакт Linux 9996273725](https://github.com/Pakka19941311/Pakka/actions/runs/34056845965/artifacts/9996273725): 613 517 байт, SHA-256 `1f0d5b34dc121e15f305b629d3d9b751dde8027b6ff1170cfecd4dcba78c980c`, отчёты и PNG. В прежнем чате ручной просмотр PNG не был выполнен; при текущем продолжении изображения получены и просмотрены. Это не подтверждает пользовательскую художественную приёмку или аппаратный FPS.

В Windows CI фактически распакован ZIP, запущены поставляемые `server.ps1` и portable Node `24.20.0`; smoke-проверка выполнена на `win32`. Подтверждены целостность файлов, запуск из другого рабочего каталога, единственный серверный процесс, выдача байтов JS и GLB, ход серверных часов без браузера, штатное завершение и сохранение того же героя после запуска из новой папки пакета.

Проверенный ZIP: `Varendor_Part1_Windows_Test_9b564e96179c.zip`, 137 943 466 байт, SHA-256 `50af1fb11af12e41f37101a41b59d6a3313a2a6f4e706604c382832264d3785f`. ZIP не опубликован и не сохранён как скачиваемый артефакт: это промежуточная проверка упаковки незавершённой части. Сохранены только свидетельства Windows-проверки — [артефакт 9996297896](https://github.com/Pakka19941311/Pakka/actions/runs/34056845965/artifacts/9996297896), 16 978 байт, SHA-256 `416c24cd49325292e43fcb9574b5df24b6013aa5f970c6b7c73f576b512c73e2`.

Промежуточный коммит `55fbae95064f4cac3fc29a01cd6a106209b1f9af` содержал неполное Git-дерево. Полное дерево восстановлено следующим обычным коммитом `9b564e96179c6222ace064a9f44a9d5d376b3fa3` и проверено: `b8aad20a8cd09caedc538784f7acdb2318c95d12`. Прежние код и ассеты сохранены; reset, force push и изменения `main` не выполнялись.

Часть 1 пока не является пользовательским тестовым релизом; GitHub Release не опубликован. Для завершения остаются четыре здания, исправление UV скалы, итоговая проверка окружения и выпуск законченного кандидата. Внешнее размещение сервера 24/7 не выполнено. Части 2 и 3 не начаты.

## История ранее сохранённых контрольных точек

Следующие сведения описывают прежние состояния проекта. Утверждения о ещё не подключённом клиенте или импорте относятся к этим контрольным точкам; актуальное состояние и проверки приведены выше.

### Окружение, карта и основание импорта до подключения клиента

- Получены согласованные исходники пяти покрытий земли, Pine Tree 01, Fern 02, Shrub 04, Rock Moss Set 01, Coastal Cliff 04 и Kloppenheim 05 Pure Sky. Реестр файлов содержит исходные URL, размеры и SHA-256. Ни один игровой файл не обращается к сайту автора во время игры.
- Подготовлены 30 вариантов GLB с двумя уровнями детализации и общими локальными текстурами. Сосны: около 22 тысяч треугольников вблизи и 7 тысяч вдали вместо миллионов в исходном glTF. Качество преобразования требует просмотра фактического изображения.
- В окружение существующей карты подключены новые природные модели, смешивание пяти покрытий, рельеф и небо.
- Получен бесплатный `castle_pack.blend` ChuckCG. Подготовлены шесть модулей, два уровня детализации и общие текстуры 1K; около 7,6 МБ игровых файлов. Новые стены, башни, арки и здания крепости подключены в Гринфолле и Астерхолде. Положение основного прохода сохранено. Контур арки рассчитан из геометрии; верхняя часть учитывается камерой и лучами, не перекрывая движение под ней.
- Исходник замка сохранён отдельно от публичных игровых производных. Лицензия автора, исходный SHA и изменения: `public/assets/licenses/CASTLE_CHUCKCG.md`. Таверна, кузница и жилые дома ещё используют прежние модели до получения четырёх согласованных файлов BlendSwap.
- Добавлен отдельный серверный процесс с сохранением SQLite, серверными часами, состоянием сущностей, намерениями перемещения/боя, предметными командами и долговечными подтверждениями операций. Добавлен клиентский транспорт потока состояния.
- Подготовлен однократный перенос старого бета-персонажа: сервер сохраняет точную исходную копию и новую запись одной транзакцией, повтор запроса возвращает того же героя. Сохраняются предметы и их заточка, валюта, опыт, квест и выданные бета-свитки; производные характеристики пересчитываются по игровым определениям. Старые универсальные свитки остаются архивным запасом без обмена. Перенос отключён по умолчанию и включается только на локальном бета-сервере; пользовательская кнопка продолжения к нему ещё не подключена.
- Добавлен единый формат статической карты: препятствия, проходы под арками и повышенные площадки. Сервер восстанавливает эти данные из офлайн-сборки, не принимает карту от подключённого игрока. Убрана зависимость поворота декораций от случайных игровых событий. Два независимых браузерных построения совпали: 471 препятствие и 2 площадки. Полный файл текущей карты сохранён в `public/assets/world/world-topology.json` (77 036 байт; SHA-256 `90a67330e9a8dc717f5ff778a731b3c80d5f304753e0e0c7020c029b50a34cd2`). После интеграции четырёх оставшихся зданий файл нужно обновить тем же способом.
- Сервер пока **не подключён к игровому клиенту как рабочий режим**. Его локальный процесс не является внешним сервером 24/7. Нет основания объявлять браузерную игру уже сетевой.

## Исторические проверки окружения и основания сервера

Текущая контрольная точка замка:

- `npm run test:castle`: 7 проверок прохода, повёрнутой арки, столкновений, камеры и видимости; PASS, около 0,2 секунды.
- `npm run build`: типы, SHA/размеры новых ресурсов, локальные ссылки GLB и production build; PASS.
- Просмотрен реальный рендер исходных модулей в Blender. Он не является изображением игры и не заменяет браузерную проверку.
- [CI замка 34048396088](https://github.com/Pakka19941311/Pakka/actions/runs/34048396088), код `809e512815dce5099a2580364dd5a384bda4b2a4`: success. Ворота, двор и столица отрисованы в Chromium software WebGL; 12 шаблонов, 30 размещений замка, ошибок загрузки нет. Артефакт 9993825539: 3 699 504 байта, SHA-256 `0121411d33b9bc0596a6b14d5db8dda0944e8641615a9a0b1b2d3d4728463cab`. Ручной просмотр этих PNG не выполнен: файловая выдача HTTP 403. Аппаратный FPS и художественная приёмка не подтверждены.

Следующая рабочая точка карты и импорта:

- 3 целевые проверки передачи геометрии и 5 проверок импорта (в том числе реальные HTTP-запросы, повтор после потерянного ответа и перезапуск SQLite) — PASS; каждый набор занимает меньше секунды.
- `npm run build` — PASS. Полный старый QA не запускался.
- [CI 34049740343](https://github.com/Pakka19941311/Pakka/actions/runs/34049740343), код `679cfea310c8d41191c466bed27dd2fb055b57ba`: success, 8 целевых проверок, build и совпадение карты двух независимых рендерящих клиентов. Для сравнения статической карты сцены строятся по очереди; первая попытка с двумя тяжёлыми software WebGL сценами одновременно прервалась на тайм-ауте загрузки второй страницы. Это не проверка уже подключённой сетевой игры: клиентский игровой цикл пока локальный.
- Карта записана штатной CI отдельным обычным коммитом `0ad458051f457da1d501eb04b33c7dc69c9868a7`; удалённый SHA проверен. Артефакт свидетельства 9994206557: 14 748 байт, SHA-256 `3db12fd6b8ff83145ba16ffd41f98c0e93618fd0e407fc952796c678b3f38c91`.
- Дополнительно фактически запущен CLI серверного процесса с полным новым файлом карты и временной SQLite: `/api/health` вернул protocol 1, импорт по умолчанию отключён; штатное завершение успешно. Это локальная техническая проверка, не внешнее размещение 24/7 и не Windows-релиз.

Ранее сохранённое свидетельство природы и основания сервера (не повторялось ради замка):

- `npm run test:part1`: 14 проверок новых серверных операций, столкновений и изменённого рельефа; PASS, около 0,5 секунды.
- `node --experimental-strip-types scripts/world-server-regression.mjs`: отдельный процесс и два HTTP-клиента, продолжение времени при остановке чтения/отключении клиентов, сохранённый срок появления, повтор операции и сессия после перезапуска; PASS.
- `npm run build`: типы, ссылки ресурсов, производственная сборка; PASS.
- Полный старый QA A–G и повтор тестов всех классов не запускались. Эти результаты ещё не подтверждают визуальное качество или FPS.

## Что остаётся внутри части 1

1. Получить четыре согласованных здания BlendSwap, запечь материалы и подготовить игровые модели; проверить лицензионные сведения каждого исходника. Замок уже получен и подключён.
2. После интеграции оставшихся зданий обновить серверный файл препятствий и проверить затронутые маршруты. Подключение игрового клиента и продолжения к серверу выполнено; подтверждённые сценарии перечислены в актуальном разделе выше.
3. Исправить UV скалы `coastal_cliff_04` из проверенного исходника. Повторить реальный вид скалы и затронутые маршруты; после новых зданий проверить итоговую архитектуру. Первые пять видов уже просмотрены на коде `305edd4b3eb02252a77322dfec776bc67b104ad4`; кроны и масштаб стволов исправлены. Привязать итоговое свидетельство к точному SHA.
4. Упаковать законченный кандидат с серверным процессом, подтвердить его Windows-запуск и выпустить в GitHub Release, выдать короткий список тестирования и остановиться до принятия. Путь упаковки уже проверен на текущей промежуточной точке; будущий итоговый ZIP должен соответствовать итоговому SHA.

## Текущее состояние доступа

- Владелец сам вошёл в BlendSwap и Gumroad в рабочем браузере. Успешный вход и страница выдачи бесплатного замка подтверждены; прежнее препятствие Google-входа устранено. Повторный вход не запрашивается.
- Связь с Cloud Browser нестабильна: список вкладок и страница Tavern 004 с подтверждённым входом снова прочитаны, но нажатие реальной кнопки Download завершилось `Input.dispatchMouseEvent timed out`, затем снова `CDP operation refresh tabs timed out after 20000ms`. Это техническая недоступность браузера, а не отказ аккаунта или новое лицензионное ограничение. Файлы House 001/002, Tavern 004 и Blacksmith 005 пока не получены; другие модели вместо согласованных не подставлены.
- Локальный адрес предпросмотра `http://127.0.0.1:5173/` недоступен подключённому браузеру (`ERR_BLOCKED_BY_CLIENT`). Это не ошибка сборки и не подтверждение runtime. Необходимо разрешённое браузерное превью либо свидетельство штатной проектной CI.
- Бесплатный вариант Medieval Castle Asset Pack получен после завершённой владельцем выдачи. Все нужные изображения упакованы внутри `.blend`; повторное скачивание отдельных текстур не потребовалось.

## Сохранение исходной контрольной точки

Код: `04d698a19007c71e7da294034ba499a8cc1f488d`. Проверки этой точки на GitHub: [run 34044509413](https://github.com/Pakka19941311/Pakka/actions/runs/34044509413), success. Подготовленные игровые файлы сохранены отдельным нормальным коммитом рабочей ветки `9d3d3dd6a42b8fa27dd8b7b64ea12a571fbd7566`. Эта первая CI проверяла сборку и серверные контракты.

Следующая игровая контрольная точка: `d9072b30afb929c7f5f85f8d7813f093c7c2d70f`. [CI 34044959905](https://github.com/Pakka19941311/Pakka/actions/runs/34044959905) завершилась `success`: 15 целевых проверок, отдельный серверный процесс с двумя клиентами, production build и три технических вида окружения в Chromium software WebGL. Во всех видах готовы земля и небо, загружены 30 шаблонов GLB; на карте 382 размещения новых природных моделей. Это не аппаратный замер FPS и не принятие художественного результата.

Артефакт свидетельств: [part1-checkpoint-evidence, 9992839327](https://github.com/Pakka19941311/Pakka/actions/runs/34044959905/artifacts/9992839327), 3 750 569 байт, SHA-256 `cb6a881ab8f426a6f2f9d5313f037a6f92c0e11d9b9af480cad36a3bfda97edd`. Содержит три PNG и машинные отчёты, не Windows-сборку игры. Ручной просмотр PNG в этой среде не выполнен: выдача через файловую ссылку вернула HTTP 403 / code 1010. Блокировка не обходилась.

Вход и выдача Gumroad больше не являются препятствием. Для четырёх файлов BlendSwap требуется восстановление связи с рабочим браузером; четыре здания и общая приёмка остаются незавершёнными. Подключение клиента к серверу и интерфейса к импорту выполнено в текущей контрольной точке, описанной в начале документа. Повторное согласование ТЗ или логин не требуются. Последняя доступная страница — `https://blendswap.com/blend/28068/download` с кнопкой `Download (5.44 MB)`; перезагрузка средствами управления браузером также вернула тайм-аут. Следующее действие доступа — восстановить связь с рабочим браузером, затем продолжить получение согласованных файлов.

Контрольные коммиты сохраняют работу, но не запускают выпуск незавершённой части под старым названием Visual G.
