# VARENDOR: продолжение в новом чате — сначала население и бой

## Последнее поручение владельца, 11.09.2026

Владелец попросил перенести работу в новый чат из-за длины старого.
Продолжать существующий проект с сохранённого места. Повторять уже готовую
обзорную сборку не нужно. **До дальнейшего украшения лесов, гор и пещер
сначала расставить монстров, подключить рабочую механику и проверить ИИ,
атаки и ошибки в новом мире. После этого — архитектура и окружение по ТЗ.**
Для проверки интегрированного населения нужна следующая игровая Windows-сборка
через Actions и GitHub Release. Не собирать повторно неизменённую прогулку.
Это изменение порядка работ разрешено владельцем; повторного согласования
плана или уже утверждённых требований не требуется. Начать реализацию,
а не закончить очередным планом.

## Где реальная работа

Абсолютный рабочий каталог прежнего чата передан приватно в стартовом
сообщении нового чата. Далее он обозначен как `${WORKSPACE}`.

**Существующий репозиторий:**
`${WORKSPACE}/work/Pakka`.

Новый чат может иметь другую начальную папку. Указывать этот существующий
репозиторий как workdir команд. Не начинать новую игру, не создавать заменяющий
checkout и не терять материализованные Blender/GLB.

GitHub: `Pakka19941311/Pakka`; единственная рабочая ветка —
`work/character-art-v3`. Последняя исходная контрольная точка перед этой передачей —
`8fbadf55698628fb75718ba4d61bc1ac777f1b79`; она включает более свежие D14–D16
из отдельного Linux checkout. Передача добавляется поверх неё без отката.
Игровой ZIP прогулки построен раньше, из `687f980175f4245c0a18fcea8c2c8828e471ce76`.
Перед началом проверить актуальный HEAD и отсутствие другого писателя.
Ветки `main` и `work/knight-integration-v2` не менять. Принятая база рыцаря —
`3897e1152b96a0f51180ba6706848b9a0e99b577`.

Сначала прочитать `WORLD_STATE.json`, этот файл, полное
`docs/world-final/spec/VARENDOR_WORLD_FINAL_TZ.md` и
`docs/world-final/spec/WORLD_REQUIREMENTS.json`; открыть само изображение
`docs/world-final/spec/reference/varendor_world_reference.png`.
В папке spec также SOURCE_NOTES.md, START_HERE.txt и хэши оригинального пакета.

Исходная передача персонажа и работающие системы описаны в
`docs/handoff/CODEX_CHARACTER_WORLD_V3_RU.md`,
`docs/migration/v2/Varendor_Migration_TZ_v2_0.md`,
`art/knight-v2/INTEGRATION.md`, `art/knight-v2/README_RU.md`,
`art/knight-v2/assets-manifest.json`.

## Промежуточный ZIP уже выдан

Сборка **687f980175f4**, Actions
<https://github.com/Pakka19941311/Pakka/actions/runs/34527455754> — оба задания
завершились успешно. Это D13 для прогулки, НЕ законченная игра в новом мире.

Прямая ссылка:
<https://github.com/Pakka19941311/Pakka/releases/download/world-walk-687f980175f4/Varendor_World_Walk_687f980175f4.zip>

Размер 1062196733 байта; SHA-256:
`a44e9bfaea5213bc1cfe1dfc1a3bb32d0f31661369a4ed342ece03d7c9ee263b`.
Распаковать и запустить `RUN_VARENDOR.bat`.

Поверхность, шахта и пещера проверены в локальном Godot с графикой и кадрами.
Сам поставляемый EXE проверен в Actions **на Windows без графического окна**:
три пространства, настоящий ввод W, остановка 0 м, пауза/возврат, данные PCK,
сохранённая навигация интерьеров. ZIP скачан обратно и проверен по SHA-256.
Это не визуальная приёмка Windows GPU и не итоговая приёмка мира.

`godot-pc/project.godot` по-прежнему открывает `res://main.tscn` со старым миром.
Временный CI-профиль использует `world-final/preview_menu.tscn` с выбором
поверхности, шахты, пещеры и быстрыми переходами по локациям.
В прогулке **нет сервера, населения, боя, NPC, инвентаря и карт**, личные
сохранения не открываются. Это главное отличие, которое теперь нужно устранить
для следующего игрового теста через существующие системы проекта.

## Состояние мира

A завершён. B — пространственная география проверена. C — геометрический
проход архитектуры и два интерьера сделаны; художественная приёмка не заявлена.
D частично готов, E не начат. F/G ещё не интегрированы, H не пройден.
**Новый приоритет: необходимая интеграция F и население G перед продолжением D/E.**

Мир 1600×1400 м, 12 основных локаций, 15 согласованных видов.
**Постоянная популяция ровно 1000 = 997 обычных + 3 босса.**
Таблица распределения по локациям в ТЗ/JSON окончательная; не перераспределять
её по своему усмотрению. 70 внутри шахты и 110 внутри пещеры уже включены
в общую сумму. Призываемые временные существа не должны незаметно добавляться
к постоянным слотам. Не возвращаться к прежним планам численности.

Авторская раскладка: `godot-pc/world-final/world_layout.json`, revision layout-06.
География D13: `godot-pc/world-final/geology-D13/`; 143 ячейки и heightmap.f32,
сетка 801×701 с шагом 2 м. Существующие дороги и реальные подъёмы сохранены.
Архитектура: `godot-pc/world-final/geography/landmarks.glb` (C09).
Интерьеры C04: `godot-pc/world-final/interiors/`, отдельные floor.f32,
коллизии, GLB и кэшированные `mine-nav.tres`, `great_cave-nav.tres`.

Координаты Godot: X на восток, Y вверх, +Z на юг.
**Серверный z = минус Godot Z.** Новую карту нельзя подключить одним общим
множителем старого мира. Старые spawn-регионы пока относятся к старой территории.

Рабочие сцены/код новой поверхности: `walk_preview.gd`, `geography_preview.gd`,
`preview_menu.gd`; интерьеры: `interior_walk_preview.gd`, `interior_preview.gd`
в `godot-pc/world-final/`. Просмотр использует существующие классы героя,
движения, камеры, анимации и коллизий.

Лес D13: 92973 размещения, в том числе 9338 деревьев; трава 362189 точек;
151 скала. Сохранить густоту. Крупные природные дефекты ещё видны:
гладкие горные грани, равномерный снег, резкие границы травы/лесных масс,
палитра земли и плоская вода. Не скрывать их туманом и не объявлять финалом.

Незавершённые кандидаты D14–D16 сохранены из актуального удалённого HEAD:

- D14: `art/world-final/Varendor_Ancient_Tree_D-14.blend`,
  `godot-pc/world-final/nature/assets/ancient_tree_D14.glb`, 54 меша и 27444 треугольника.
  Редактируемое размещение — `godot-pc/world-final/landmarks/ancient_tree.tscn`;
  маршруты — `ancient-tree-routes.json`. Численные проходы мотором в обе стороны,
  периметр и сохранение/повторное открытие размещения проверены. Это headless;
  игровые кадры не просмотрены, графическая приёмка и интеграция в main не выполнены.
  Свидетельства: `docs/world-final/evidence/D14-tree/`.
- D15: `godot-pc/world-final/materials/surface-profile-D15.json`,
  `terrain_D15.gdshader`, `godot-pc/world-final/surface_D15_review.tscn`.
  Кандидат материалов склонов, снега и почвы; импорт Godot 4.6.3 прошёл,
  графическое качество и соответствие Blender ещё не проверены, в основную игру не перенесён.
- D16: `godot-pc/world-final/nature/assets/swamp_water_D16.glb`,
  `landmarks/swamp_water.tscn`, `materials/swamp_water_D16.gdshader`.
  18179 вершин, 33153 треугольника; рельеф и коллизии не изменены.
  Импорт и отдельная загрузка сцены прошли. Реальный Blender-исходник ещё требуется
  завершить и проверить; визуальная проверка и интеграция не выполнены.

В отдельной Linux-среде графика была недоступна. Actions 34532322276 завершился
по таймауту, Release для его диагностики отклонён HTTP 403; это не успешная приёмка.
Следующий run 34533475898 был запущен для D14/D15: при необходимости проверить
его актуальный статус, не считать заранее успешным и не запускать повторно без причины.
Не потерять эти изменения при синхронизации. Доводку D14–D16 отложить до проверки
населения согласно последнему поручению. В выданный D13 ZIP они не включены.

## Первое практическое действие в новом чате

Проверить ветку/изменения и отсутствие второго писателя в этот репозиторий.
Затем разобрать точку подключения нового мира к **существующей** серверной
симуляции и Godot-клиенту, сразу выполнять необходимую интеграцию.
Составить реальные данные размещения по утверждённой таблице и подключить
существующие визуальные модели, ИИ, атаки, урон, смерть, дроп и возрождение.
Пустые маркеры или визуальные копии без серверного поведения не являются результатом.

Искать актуальные определения в:
- `src/data/game-data.ts` — MONSTERS и действующие статы/модели;
- `src/world/spawn-regions.ts`, `monster-ai.ts`, `navigation.ts`,
  `collision-world.ts`, `terrain-surface.ts`, `world-topology.ts`;
- `src/server/world-simulation.ts`, `src/core/monster-lifecycle.ts`;
- `src/server/book-system.ts`, `src/data/loot-v3.ts`;
- `server/world-stream.mjs`, `server/world-store.mjs`, `server/http-server.mjs`;
- `godot-pc/scripts/main.gd`, `world.gd`, `network.gd`, `animation_controller.gd`;
- `docs/content-expansion-v3/APPROVED_TZ_RU.md`, `ASSETS_APPROVED_RU.md`,
  `release-evidence/monster-art.json` и соответствующие manifests ассетов.

Подсказка по существующим ID (обязательно сверить по файлам):
`fire_golem`, `ice_golem`, `rift_boss`, `night_zombie`, `night_skeleton`,
`wolf`, `exile`, `spider`, `undead`, `bat`, `cultist`, `miner`, `wraith`,
`mini`, `big`. Исторический `wolf` использует лису, `spider` — слизня;
`mini` — массивный оборотень, `big` — Хозяин Гнилого Леса.
Не переименовывать ID ради визуального названия. Не заменять одобренные модели.

Проверки затронутого блока: точные суммы по локациям и пространствам,
отсутствие дублей постоянных UID, безопасные точки на земле и внутри интерьеров,
разброс без куч, агр/погоня/возврат, обход препятствий, подъёмы, дистанция и
момент урона атак, автоатака, смерть/дроп/возрождение, отсутствие размножения
при выгрузке/перезаходе. Проверить реальные сцены в движении и нормальный
игровой бой. Серверный учёт 1000 не означает рендер/полный ИИ всех 1000 каждый кадр.
Не уменьшать население или лес ради производительности.

Сохранить героя, управление без скольжения, камеру, пять последовательных
ударов автоатаки без отдельных кликов, сменную экипировку, UI, NPC-сервисы,
инвентарь, заточку, кладовку, предметы, уровень/XP и сохранения.
Не менять серверную скорость атаки/урон/баланс под анимацию.
Переходы между пространствами не должны снова сбрасывать уровень героя.
Тестировать конкретные зависимости изменения; не повторять всю миграцию.
После интеграции и проверок — commit/push и игровая Windows-сборка через
Actions/Release, затем вернуться к архитектуре и природе по полному ТЗ.

## Инструменты, сохранность и Git

От корня прежнего рабочего каталога инструменты находятся в `work/tools/`:
Godot `Godot_v4.6.3-stable_win64_console.exe`, Blender
`blender-4.2.3-windows-x64/blender.exe`, Node
`node-v24.20.0-win-x64/node.exe` (для этого окружения при необходимости
`--preserve-symlinks --preserve-symlinks-main`).
Python: `%USERPROFILE%/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/python.exe`,
запускать с `-X utf8`. Git установлен в стандартной папке Program Files/Git.

Все реальные native/GLB уже материализованы и сохранены в 10 проверяемых
слоях `art/world-final/payload-manifest.json`: 927 файлов, 246 частей.
`scripts/world_final/materialize_payload.py` восстанавливает отсутствующие
файлы с проверкой хэшей; не обходить защиту вручную изменённых исходников.
Не заменять native только генератором. Бэкапы вне репозитория в старом
рабочем каталоге: `outputs/world-preview-recovery`, `world-final-D13-in-progress`,
`world-native-D13`, `world-preview-private-diagnostics`; старые копии сохранены.

Native Git push в этой среде дважды завершался exit 1 без диагностики.
Рабочий способ публикации — GitHub MCP create_blob/create_tree/create_commit/
update_ref с `force:false`, только рабочая ветка. Затем обычный fetch и безопасная
синхронизация локального ref/index после проверки совпадения файлов.
Не передавать секреты, cookies или сырой локальный диагностический журнал в Git.
Не менять настройки защиты/системы без согласования.

**Не пушить приватные локальные checkpoint refs/history**:
`refs/checkpoints/world-walk-local-private-884c8fe`,
`refs/checkpoints/world-walk-before-remote-sync-746456d` и их приватные предки.
Один сырой локальный отчёт ранее был отклонён авто-проверкой публикации;
его и полные локальные журналы сохранили вне Git, публичные отчёты очищены.
Безопасные изменения уже опубликованы отдельной цепочкой от 6fd7d6a.

При передаче есть 7 старых диагностических текстов в
`docs/world-final/diagnostics/godot-20260910/`, показывающихся изменёнными
только из-за переводов строк (`git diff --ignore-space-at-eol` пуст).
Не включать их в коммит. Есть старые неотслеживаемые *.gd.uid, haste.svg.import
и scripts/__pycache__; не использовать слепое `git add .`.

Запуски Godot делать через `scripts/godot_run_checked.py`: он заранее создаёт
записываемый абсолютный log-file, фиксирует код/таймаут и память. Это обратимое
исправление подтверждённого падения RotatedFileLogger; не менять renderer и
не удалять рыцаря. Headless не выдавать за визуальную проверку.
Известные нефатальные Windows certificate/shader-cache сообщения не скрывать.

Ошибка первого CI исправлена: **у Godot нет флага `--license`**.
Он запускал незавершающийся процесс после готового экспорта. Отменённый run
34523422720 не считается успешным; описание `docs/world-final/diagnostics/WALK_BUILD_RU.md`.
Теперь `scripts/world_final/write_godot_license.gd` извлекает лицензии через API
и явно завершает процесс; повторный run 34527455754 целиком успешен.
`.github/workflows/godot-world-walk.yml` — именно обзорный профиль без сервера;
для следующей боевой сборки правильно подключить нужный игровой runtime,
а не просто запустить эту же прогулку ещё раз.

После создания нового чата прежний агент прекращает записи в репозиторий.
Продолжать работу в новом чате самостоятельно, без повторного согласования.
