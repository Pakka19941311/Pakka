# Задержка первой промежуточной сборки

Задание export в [Actions 34523422720](https://github.com/Pakka19941311/Pakka/actions/runs/34523422720/job/103026345185)
остановлено для диагностики, результат — `cancelled`, а не успешная сборка.
Сборочный коммит: `8ef78c79e36bc58de2a9f436216c4d40fba9f8db`.

Журнал показывает начало импорта 10.09.2026 в 20:01:57 UTC, начало
экспорта в 20:05:37 и `[ DONE ] savepack` в 20:05:44. Затем вывода
не было до отмены в 20:32:33. После экспорта workflow вызывал
`Godot --headless --license`, перенаправляя вывод в файл лицензии.

В справке именно Godot 4.6.3 такого аргумента нет. Контролируемый
запуск `--headless --license` вне проекта тоже не завершился за 5 секунд
и был остановлен. Это подтверждённая ошибка служебного шага workflow.
Данных о нехватке памяти, повреждении ресурсов или аварии экспортёра
в этом журнале нет. Персонаж, модель, графический режим и мир не менялись.

Исправление: `scripts/world_final/write_godot_license.gd` получает лицензию
и уведомления сторонних библиотек через API самого двигателя и явно
завершает процесс. Локальная проверка: Godot 4.6.3, без графического окна,
exit 0 за 0,422 секунды. Это служебная проверка, не визуальная приёмка.
Импорт, экспорт и получение лицензий разделены на шаги с ограничением
времени; успешная упаковка сама по себе не разрешает публикацию ZIP.
Перед ней обязательна проверка экспортированного EXE на Windows.

Исходные журналы локального повторения сохранены вне репозитория
в `outputs/world-walk-ci-diagnosis` относительно рабочего каталога Codex.
Полный журнал CI доступен по ссылке на задание выше.

Повторный [run 34527455754](https://github.com/Pakka19941311/Pakka/actions/runs/34527455754)
с коммитом `687f980175f4245c0a18fcea8c2c8828e471ce76` завершился успешно:
импорт, экспорт, лицензии, три запуска поставляемого EXE на Windows,
упаковка, публикация и обратное скачивание со сверкой SHA-256.
Результат — `docs/world-final/evidence/walk-preview/windows-release.json`.
