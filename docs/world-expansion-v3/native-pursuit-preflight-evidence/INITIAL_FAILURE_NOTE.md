The first server preflight failed the over-strict requirement that a 150 ms observation itself see the monster strictly outside the leash radius. All four traces show the native AI returning immediately near that boundary. The second test requires the hero beyond leash, monster reaching the boundary within 0.3 seconds of normal movement, then observed return and target release. The first process used the earlier script loaded in memory; its on-disk end-of-run source hashes were captured after the evaluation code had been edited. It is retained as failure history, not accepted source-verified evidence. The second run verifies unchanged source hashes from start to end. Neither report establishes native rendering.
