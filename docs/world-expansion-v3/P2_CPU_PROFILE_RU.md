# Измерение CPU в действующем лесном участке P2

## Исходный прогон 01

12 сентября 2026, один `stage_qa.mjs --starter-v3 --block=p2-cpu` через `godot_run_checked.py`. NVIDIA GeForce RTX 3070 Laptop GPU, окно 1600×900, существующий high profile: quality/distance/vegetation/render_scale/MSAA=2, shadows=true. Клиентская и серверная карты совпали; generated-файлы не менялись.

Использован существующий `p2-nature-forest` fixture: только начальная позиция героя и стартерная экипировка. 1151 место, монстры и часы не изменены. После fixture: 5 секунд прогрева, 8 секунд покоя, 10 секунд наблюдения после одной обычной команды destination до принятой верхней точки. Герой прошёл 34,84 м с неизменной generation. В конце окна он уже достиг цели; окно не выдаётся за десять секунд непрерывной ходьбы.

Checked run: exit 0, 68,016 с, 6/6 проверок; crash=false. Прежние сообщения shader-cache и root certificate store сохранены в `p2-cpu-baseline/native-result.json`: лог не объявляется чистым.

## Что измерено

Все интервалы **включают вложенные вызовы**. `world.process` содержит actor loop и apply_snapshot; apply_snapshot содержит main.present_snapshot; actor loop содержит rig.total, а тот — controller/ground-fit. Их нельзя складывать. Отдельно network.consume_stream включает вызванные им timeline.ingest/player.reconcile. Per-frame bucket использует Engine.get_process_frames и может содержать несколько physics ticks; начальный/последний bucket фазы могут быть неполными. Данные Performance явно отмечены как относящиеся к предыдущему кадру движка. Основной wall-интервал измерен у конца world._process, а не в 100-мс QA таймере.

| Измерение | Покой 8 с | Destination 10 с |
|---|---:|---:|
| Rendered frames | 59 | 39 |
| Актёры, median / max | 179 / 179 | 202 / 205 |
| Монстры в snapshot, median | 146 | 155 |
| Wall frame median / p95, мс | 131,24 / 198,13 | 242,27 / 565,77 |
| world.process, сумма / p95 bucket, мс | 3396,79 / 78,54 | 3032,28 / 107,01 |
| world.actor_loop, сумма / p95 bucket, мс | 2258,59 / 58,32 | 1919,46 / 66,61 |
| rig.ground_fit, сумма / p95 bucket, мс | 1329,89 / 39,09 | 1181,33 / 43,85 |
| rig.controller, сумма / p95 bucket, мс | 229,66 / 6,56 | 227,91 / 8,52 |
| ambient.physics, сумма / p95 bucket, мс | 981,58 / 28,64 | 2909,27 / 147,85 |
| network.consume_stream, сумма / p95 bucket, мс | 1420,60 / 44,43 | 1518,06 / 46,20 |
| timeline.advance, сумма, мс | 377,68 | 268,76 |
| timeline.ingest, сумма, мс | 117,29 | 75,96 |
| main.present_snapshot, сумма, мс | 65,18 | 45,22 |

Самый дорогой измеренный отдельный вызов при движении — `ambient.physics`: **274,663 мс**. В кадре 940 было 16 physics ticks, суммарно 347,766 мс ambient при wall 565,767 мс. В кадре 937 — 30 physics ticks. Это подтверждает стоимость всей фоновой симуляции, но пока не отделяет A* от прочих операций внутри неё.

Код направляет следующую проверку к `ambient_residents.gd:_navigate`: синхронный `find_path(maxVisited=4500)` и бюджет два поиска, заново выдаваемый на каждом physics tick. Лесная точка находится примерно в 193 м от городского центра, поэтому попадает внутрь действующего `ambient_active` радиуса 210 м. Конкретный неисполнимый маршрут и виновный житель этим первым прогоном **ещё не установлены**.

Посадка на грунт — отдельная заметная постоянная стоимость; сама выборка анимаций существенно дешевле. На входе в лес создано 126 актёров и выполнено 247 bind: deep copies библиотек анимаций стоили 659,911 мс. Во время destination созданы 26 актёров / 52 bind, 182,300 мс bind суммарно. Это отдельные входные пики, а не объяснение всей фоновой нагрузки.

## Диагностические изменения

`p2_cpu_trace.gd` выключен по умолчанию (`enabled=false`). Включает его только явный QA block `p2-cpu`; он хранит времена и счётчики в памяти, не обходит вершины/меши, не пишет файлы и не выводит лог в измеряемом кадре. Снимок JSON пишется после отключения сбора. Таймеры в world/main/network/timeline/animation_controller не меняют clock, pose, коллизии, команды или population. Дополнительная стоимость вызовов самого инструмента входит в измерения; точная её величина отдельным A/B ещё не оценена.

Профайлер сохранён отдельным checkpoint **до оптимизаций** по прямому поручению ведущего агента. Он нужен для повторного сравнения тех же участков. Первый прогон не является приёмкой FPS, packaged EXE или исправленного маршрута.

Воспроизведение из Pakka, при свободном engine slot:

```powershell
$env:PYTHON = '<проверенный Python exe>'
node --experimental-strip-types scripts/world_final/stage_qa.mjs '<Godot console exe>' work/qa/p2-cpu-profile-NEW --starter-v3 --block=p2-cpu
```

`stage_qa.mjs` сам использует checked wrapper с заранее созданным логом. `p2_nature_acceptance.gd` этим блоком не изменялся. Полный профиль, результаты проверок и результат checked запуска находятся в `p2-cpu-baseline/`; исходный рабочий каталог — `work/qa/p2-cpu-profile-01/`.

## Диагностика конкретных маршрутов, прогон 02

Тот же forest/high сценарий выполнен ещё один раз с выключаемым таймером непосредственно вокруг `VarendorNavigation.find_path`. У каждого запроса сохранены resident ID, имя, номер точки, точные start/destination, содержимое точки расписания, полный найденный путь, результат, длительность и номер кадра. Геометрия, координаты маршрутов, расписание, лимит поиска и бюджет NPC не менялись. Изменений support-height ещё не было.

Checked run `work/qa/p2-cpu-profile-02-ambient/`: exit 0, 66,36 с, 6/6, crash=false. Сообщения shader-cache/root certificate сохранены. Данные: `p2-ambient-baseline/`.

**Гипотеза повторных безуспешных поисков не подтвердилась:** 51/51 запрос успешен. Это 49 сочетаний resident/route_index: у ambient:128 и ambient:129 точка 0 запрошена дважды, оба раза успешно; остальные сочетания — по одному разу. Общие координаты и все 39 жителей сохраняются.

| Житель / индекс точки | Фактический start → destination | Найденный путь | Событие elapsed_ms |
|---|---|---|---:|
| ambient:114, Садовница Лина / 2 | (−156,33235; −140) → (−140,5; −130) | Через (−140,83235; −136,40000), затем цель; успех | 253,266 |
| ambient:121, Подавальщица Хельга / 2 | (−144,15352; −203,69310) → (−157; −208) | Через (−151,70000; −207,80000), затем цель; успех | 96,633 |
| ambient:115, Горожанин Освин / 1 | (−118; −154) → (−109; −149) | Через (−112,70000; −153,80000), затем цель; успех | 64,092 |
| ambient:111, Подмастерье Йорен / 1 | (−154; −168,80000) → (−157; −172) | Через (−157,64999; −167,55000) и (−158,5; −170,95); успех | 34,772 |

В JSON события `elapsed_ms` включают ещё небольшую стоимость записи таймера; поэтому, например, у Лины событие показывает 253,266 мс, а непосредственный timer — 253,240 мс.

За destination-фазу вся ambient.physics стоила **2947,530 мс**, из неё 22 вызова find_path — **427,518 мс, 14,5%**. A* подтверждён как источник крупнейшего отдельного пика, но не основной суммы постоянной нагрузки. Остальное время нельзя автоматически приписать поиску пути: туда входят движение, проверки коллизии/проходимости и соседей на 594 physics ticks.

Следующее сравнение — точный spatial index support-height ведущего агента: его вызовы лежат и в обычной проверке проходимости, и внутри A*. До этого измерения рабочие маршруты и NPC budget оставлены без изменений; кооперативная A* не внедрялась.

## Сравнение после support index, прогон 03b

`work/qa/p2-cpu-profile-03b-support-index/`: 12 сентября 2026, начало 16:44:28 UTC; exit 0, 66,453 с, 6/6, crash=false. Данные и SHA входных исходников сохранены в [p2-cpu-support-index](p2-cpu-support-index/comparison.json). Сравниваем с прогоном 02: диагностические hooks одинаковы, настройки high, карта, популяция и destination совпадают. Это один замер до и один после, а не статистика нескольких повторов. Между ними также добавлены неактивные в этом лесном сценарии line/counter компоненты; соответствующие SHA перечислены в input-sources.json. Во время измерения runtime был заморожен.

| Измерение | Покой: 02 → 03b | Destination: 02 → 03b |
|---|---:|---:|
| Наблюдаемое окно, мс | 8002,664 → 8030,760 | 10080,945 → 10057,305 |
| Rendered frames | 57 → 90 | 36 → 72 |
| Wall frame median, мс | 134,682 → 88,963 | 260,306 → 127,105 |
| Wall frame p95, мс | 223,614 → 118,450 | 521,840 → 221,092 |
| Wall frame max, мс | 330,784 → 182,832 | 730,438 → 648,514 |
| Draw calls, median | 2920 → 2920 | 2932 → 2932 |
| Актёры, median / max | 179 / 179 → 179 / 179 | 202 / 205 → 204 / 205 |
| Монстры snapshot, median | 146 → 146 | 155 → 155 |
| Ground-fit mean на вызов, мс | 0,30247 → 0,05353 | 0,30221 → 0,05572 |
| Ground-fit сумма, мс | 1258,582 → 264,819 | 1165,322 → 377,610 |
| Ambient mean на physics tick, мс | 1,98641 → 0,93922 | 4,96217 → 2,28439 |
| Ambient сумма, мс | 951,489 → 453,643 | 2947,530 → 1366,066 |
| A*: число вызовов / сумма, мс | 10 / 87,509 → 10 / 30,830 | 22 / 427,518 → 22 / 181,634 |
| Actor loop mean на rendered frame, мс | 37,680 → 13,526 | 52,637 → 19,525 |
| World process mean на rendered frame, мс | 57,027 → 31,279 | 84,132 → 42,822 |
| Network consume mean на rendered frame, мс | 25,609 → 16,383 | 39,703 → 24,790 |

Стоимость ground-fit на вызов снизилась примерно в 5,4–5,7 раза. При этом вызовов стало больше из-за более частых кадров: в destination 3856 → 6777. Поэтому отношение суммарных миллисекунд не равно ускорению одного вызова. Аналогично world.process за десятисекундное окно сохранил около 3 с суммарной работы, обслужив вдвое больше кадров. Складывать его с actor loop и ground-fit нельзя: это вложенные интервалы.

**Все 51/51 запросы пути успешны; start, destination и полный path всех событий совпали с 02 в том же порядке.** Географию города, расписание и 39 жителей не меняли. Непосредственный поиск пути Лины стал 105,488 мс вместо 253,240 мс. Путь Хельги в событии elapsed стал 44,862 вместо 96,633 мс; Освина — 22,368 вместо 64,092 мс. Это сохраняет прежнее различие между прямым таймером и событием, включающим запись таймера.

После изменения крупнейшая средняя измеренная группа на rendered frame — world.process (42,822 мс в destination, включая 19,525 мс actor loop). Network consume занимает в среднем 24,790 мс, ambient — 2,284 мс на каждый physics tick. A* остаётся отдельным источником пиков, однако занимает только 181,634 из 1366,066 мс всей ambient-фазы. Ground-fit больше не объясняет основную измеренную сумму.

**Приёмка плавности остаётся открытой.** Максимальный интервал 648,514 мс возник в кадре 982: A* занял лишь 0,588 мс, world.physics — 53,762 мс (30 ticks), world.process — 48,230 мс, network consume — 43,749 мс. Эти измеренные участки не объясняют весь wall-интервал. Остаток может лежать в рендере, других стадиях кадра или планировании; данный профайлер не устанавливает конкретную причину. Previous-engine Performance метрики не подменяют wall-измерение и не суммируются как текущий кадр.

Герой снова прошёл 34,840 м до той же точки, generation стабильна. Этот CPU-блок не измеряет excess step, не проходит весь nature route и не закрывает его приёмку. Следующий согласованный этап — один полный действующий маршрут с контролем движения героя; дополнительные сравнения CPU этим результатом не запрашиваются.

### Сохранённая неудачная попытка 03

Предшествующий `work/qa/p2-cpu-profile-03-support-index/` завершён до fixture из-за parse error: новая зависимость `VarendorLineEffects` в book_ground_effects.gd ещё не разрешалась через global-class cache. Агент остановил только собственный PID 4464 после проверки полного пути процесса. Checked wrapper пометил exit 0xFFFFFFFF как crash_detected=true, но этот флаг относится к ручному Stop-Process; наблюдаемая native access violation не зафиксирована. Полный лог сохранён в work, компактная причина и SHA — в [failed03-summary.json](p2-cpu-support-index/failed03-summary.json). Данных производительности у попытки 03 нет, в таблицу она не включена. После explicit preload ведущий агент отдельно проверил stage/main, затем разрешил 03b. Ошибка зависимости не приписывается support index.
